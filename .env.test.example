# =============================================================================
# Test Environment Configuration
# Story: 0-14 Test Database Provisioning
#
# Copy this file to .env.test and fill in the values.
# NEVER commit .env.test — it is listed in .gitignore.
#
# For local development (Docker/Podman):
#   The defaults below work with the test database container.
#
# For CI/CD (GitHub Actions):
#   These values are set automatically via service containers.
# =============================================================================

# --- Test Database -----------------------------------------------------------
# PostgreSQL connection string for the test database.
# Local development: connect to Docker/Podman container on port 5433.
# CI/CD: port 5432 (GitHub Actions service container).
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5433/qualisys_test

# --- Application Database (same as TEST_DATABASE_URL for test runs) ----------
DATABASE_URL=postgresql://test_user:test_password@localhost:5433/qualisys_test

# --- Redis (optional for integration tests) ----------------------------------
REDIS_URL=redis://localhost:6379

# --- Node Environment --------------------------------------------------------
NODE_ENV=test

# --- Test Tenant IDs ---------------------------------------------------------
# Used by integration tests to set app.current_tenant for RLS testing.
TEST_TENANT_1_ID=11111111-1111-1111-1111-111111111111
TEST_TENANT_2_ID=22222222-2222-2222-2222-222222222222
TEST_TENANT_3_ID=33333333-3333-3333-3333-333333333333
